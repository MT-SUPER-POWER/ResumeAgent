apiVersion: v1
kind: Service
metadata:
  name: resume-agent-backend
  namespace: resume-agent
spec:
  type: LoadBalancer
  selector:
    app: resume-agent-backend
  ports:
    - port: 30080
      targetPort: http
      protocol: TCP
      name: http
