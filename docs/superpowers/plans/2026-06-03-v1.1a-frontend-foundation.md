# V1.1A Frontend Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the V1.1A frontend foundation: Next.js app shell, API client, JWT login, session recovery, and role-aware navigation.

**Architecture:** Start from an empty `repo/frontend/` and selectively reuse the reference dashboard structure in `docs/references/next-shadcn-admin-dashboard/`. Keep V1.1A focused on app foundation and authentication; V1.1B/C/D will add business pages after this foundation is verified.

**Tech Stack:** Next.js App Router, React, TypeScript, Tailwind CSS, shadcn-style components, Zustand, Zod, Vitest, Biome.

---

## Scope

This plan implements only V1.1A:

- Frontend project skeleton in `repo/frontend/`.
- AppShell with Navbar, Sidebar, and protected dashboard layout.
- API Client for `/api/v1/*` response envelope and JWT injection.
- Auth login, logout, and `/auth/me` session recovery.
- Role-aware navigation for `user` and `admin`.

V1.1B/C/D are intentionally excluded:

- Resume upload and SSE progress.
- Job records and candidate evaluation.
- Excel export.
- Model, user, and system admin forms.

---

## File Structure

Create these files under `repo/frontend/`:

```text
repo/frontend/
├── package.json
├── next.config.mjs
├── postcss.config.mjs
├── tsconfig.json
├── vitest.config.ts
├── biome.json
├── components.json
├── src/
│   ├── app/
│   │   ├── globals.css
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── login/page.tsx
│   │   └── dashboard/
│   │       ├── layout.tsx
│   │       ├── page.tsx
│   │       ├── analysis/page.tsx
│   │       ├── jobs/page.tsx
│   │       ├── candidates/page.tsx
│   │       ├── jds/page.tsx
│   │       ├── reports/page.tsx
│   │       ├── data/page.tsx
│   │       └── settings/page.tsx
│   ├── components/
│   │   ├── auth/login-form.tsx
│   │   ├── layout/app-shell.tsx
│   │   ├── layout/app-sidebar.tsx
│   │   ├── layout/top-nav.tsx
│   │   ├── layout/protected-route.tsx
│   │   └── ui/
│   │       ├── button.tsx
│   │       ├── input.tsx
│   │       └── badge.tsx
│   ├── config/
│   │   ├── app-config.ts
│   │   └── navigation.ts
│   ├── lib/
│   │   ├── api-client.ts
│   │   ├── auth-store.ts
│   │   ├── auth-types.ts
│   │   └── utils.ts
│   └── test/
│       └── setup.ts
└── tests/
    ├── api-client.test.ts
    ├── auth-store.test.ts
    └── navigation.test.ts
```

Responsibilities:

- `src/lib/api-client.ts`: fetch wrapper, response envelope parsing, token injection, typed errors.
- `src/lib/auth-store.ts`: client-side auth state, token persistence, login/logout/session recovery.
- `src/config/navigation.ts`: role-aware sidebar items.
- `src/components/layout/*`: global shell and protected layout.
- `src/components/auth/login-form.tsx`: login UI and error display.
- `src/app/*`: routes only; keep business logic in `src/lib` or focused components.

---

## Task 1: Initialize Frontend Tooling

**Files:**

- Create: `repo/frontend/package.json`
- Create: `repo/frontend/next.config.mjs`
- Create: `repo/frontend/postcss.config.mjs`
- Create: `repo/frontend/tsconfig.json`
- Create: `repo/frontend/vitest.config.ts`
- Create: `repo/frontend/biome.json`
- Create: `repo/frontend/components.json`
- Create: `repo/frontend/src/test/setup.ts`

- [ ] **Step 1: Create package manifest**

Use the reference template dependency style, but keep only foundation dependencies:

```json
{
  "name": "resume-agent-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "biome lint",
    "format": "biome format --write",
    "check": "biome check",
    "check:fix": "biome check --write",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@hookform/resolvers": "^5.4.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.17.0",
    "next": "^16.2.7",
    "next-themes": "^0.4.6",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-hook-form": "^7.77.0",
    "sonner": "^2.0.7",
    "tailwind-merge": "^3.6.0",
    "zod": "^4.4.3",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@tailwindcss/postcss": "^4.3.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.0",
    "@types/node": "^22.19.19",
    "@types/react": "^19.2.16",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.1",
    "jsdom": "^27.3.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^4.1.5",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}
```

- [ ] **Step 2: Create config files**

Use these minimal configs:

```js
// next.config.mjs
const nextConfig = {};

export default nextConfig;
```

```js
// postcss.config.mjs
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
```

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

```ts
// vitest.config.ts
import react from "@vitejs/plugin-react";
import { defineConfig } from "vitest/config";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: ["./src/test/setup.ts"],
  },
  resolve: {
    alias: {
      "@": new URL("./src", import.meta.url).pathname,
    },
  },
});
```

```json
{
  "$schema": "https://biomejs.dev/schemas/2.4.16/schema.json",
  "formatter": { "enabled": true, "indentStyle": "space", "indentWidth": 2 },
  "linter": { "enabled": true, "rules": { "recommended": true } },
  "javascript": { "formatter": { "quoteStyle": "double", "semicolons": "always" } }
}
```

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

```ts
// src/test/setup.ts
import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 3: Install dependencies**

Run:

```bash
cd repo/frontend
npm install
```

Expected: `package-lock.json` is created and install exits with code 0.

- [ ] **Step 4: Run baseline checks**

Run:

```bash
cd repo/frontend
npm run check
npm run test
```

Expected: both commands exit with code 0. `npm run test` reports no test files only if no tests have been added yet; after Task 2 it must run real tests.

- [ ] **Step 5: Commit**

```bash
git add repo/frontend/package.json repo/frontend/package-lock.json repo/frontend/next.config.mjs repo/frontend/postcss.config.mjs repo/frontend/tsconfig.json repo/frontend/vitest.config.ts repo/frontend/biome.json repo/frontend/components.json repo/frontend/src/test/setup.ts
git commit -m "build(web): 初始化前端工程"
```

---

## Task 2: Add API Client With Envelope Parsing

**Files:**

- Create: `repo/frontend/src/lib/api-client.ts`
- Test: `repo/frontend/tests/api-client.test.ts`

- [ ] **Step 1: Write failing tests**

```ts
// tests/api-client.test.ts
import { beforeEach, describe, expect, it, vi } from "vitest";
import { ApiError, createApiClient } from "@/lib/api-client";

describe("api client", () => {
  beforeEach(() => {
    vi.restoreAllMocks();
  });

  it("unwraps successful response envelope", async () => {
    vi.spyOn(globalThis, "fetch").mockResolvedValue(
      new Response(JSON.stringify({ code: 0, msg: "ok", data: { token: "jwt" } }), {
        status: 200,
        headers: { "Content-Type": "application/json" },
      })
    );

    const client = createApiClient({ baseUrl: "http://localhost:8080/api/v1" });
    await expect(client.post<{ token: string }>("/auth/login", { username: "admin", password: "admin123" })).resolves.toEqual({
      token: "jwt",
    });
  });

  it("throws ApiError for business error envelope", async () => {
    vi.spyOn(globalThis, "fetch").mockResolvedValue(
      new Response(JSON.stringify({ code: 30102, msg: "用户名或密码错误", data: null, detail: null }), {
        status: 200,
        headers: { "Content-Type": "application/json" },
      })
    );

    const client = createApiClient({ baseUrl: "http://localhost:8080/api/v1" });
    await expect(client.get("/auth/me")).rejects.toMatchObject({
      code: 30102,
      message: "用户名或密码错误",
    });
  });

  it("injects bearer token when token provider returns a value", async () => {
    const fetchMock = vi.spyOn(globalThis, "fetch").mockResolvedValue(
      new Response(JSON.stringify({ code: 0, msg: "ok", data: { id: "1" } }), {
        status: 200,
        headers: { "Content-Type": "application/json" },
      })
    );

    const client = createApiClient({
      baseUrl: "http://localhost:8080/api/v1",
      getToken: () => "abc",
    });

    await client.get("/auth/me");
    const headers = fetchMock.mock.calls[0]?.[1]?.headers as Headers;
    expect(headers.get("Authorization")).toBe("Bearer abc");
  });

  it("exposes response metadata on ApiError", () => {
    const error = new ApiError(10001, "缺少必填字段", { field: "username" });
    expect(error.code).toBe(10001);
    expect(error.detail).toEqual({ field: "username" });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
cd repo/frontend
npm run test -- tests/api-client.test.ts
```

Expected: FAIL because `@/lib/api-client` does not exist.

- [ ] **Step 3: Implement API client**

```ts
// src/lib/api-client.ts
type ApiEnvelope<T> = {
  code: number;
  msg: string;
  data: T | null;
  detail?: unknown;
};

type ApiClientOptions = {
  baseUrl: string;
  getToken?: () => string | null;
};

type RequestOptions = {
  method?: "GET" | "POST" | "PUT" | "DELETE";
  body?: unknown;
  headers?: HeadersInit;
};

export class ApiError extends Error {
  readonly code: number;
  readonly detail: unknown;

  constructor(code: number, message: string, detail?: unknown) {
    super(message);
    this.name = "ApiError";
    this.code = code;
    this.detail = detail;
  }
}

export function createApiClient(options: ApiClientOptions) {
  async function request<T>(path: string, requestOptions: RequestOptions = {}): Promise<T> {
    const headers = new Headers(requestOptions.headers);

    if (!(requestOptions.body instanceof FormData)) {
      headers.set("Content-Type", "application/json");
    }

    const token = options.getToken?.();
    if (token) {
      headers.set("Authorization", `Bearer ${token}`);
    }

    const response = await fetch(`${options.baseUrl}${path}`, {
      method: requestOptions.method ?? "GET",
      headers,
      body:
        requestOptions.body instanceof FormData
          ? requestOptions.body
          : requestOptions.body === undefined
            ? undefined
            : JSON.stringify(requestOptions.body),
    });

    if (!response.ok) {
      throw new ApiError(response.status, response.statusText);
    }

    const envelope = (await response.json()) as ApiEnvelope<T>;
    if (envelope.code !== 0) {
      throw new ApiError(envelope.code, envelope.msg, envelope.detail);
    }

    return envelope.data as T;
  }

  return {
    get: <T>(path: string) => request<T>(path),
    post: <T>(path: string, body?: unknown) => request<T>(path, { method: "POST", body }),
    put: <T>(path: string, body?: unknown) => request<T>(path, { method: "PUT", body }),
    delete: <T>(path: string) => request<T>(path, { method: "DELETE" }),
  };
}
```

- [ ] **Step 4: Run tests and checks**

Run:

```bash
cd repo/frontend
npm run test -- tests/api-client.test.ts
npm run check
```

Expected: both commands exit with code 0.

- [ ] **Step 5: Commit**

```bash
git add repo/frontend/src/lib/api-client.ts repo/frontend/tests/api-client.test.ts
git commit -m "feat(web): 增加 API 客户端"
```

---

## Task 3: Add Auth Store and Session Recovery

**Files:**

- Create: `repo/frontend/src/lib/auth-types.ts`
- Create: `repo/frontend/src/lib/auth-store.ts`
- Test: `repo/frontend/tests/auth-store.test.ts`

- [ ] **Step 1: Write failing tests**

```ts
// tests/auth-store.test.ts
import { beforeEach, describe, expect, it, vi } from "vitest";
import { createAuthStore } from "@/lib/auth-store";
import type { CurrentUser } from "@/lib/auth-types";

describe("auth store", () => {
  beforeEach(() => {
    localStorage.clear();
    vi.restoreAllMocks();
  });

  it("logs in and persists token", async () => {
    const user: CurrentUser = { id: "u1", username: "admin", role: "admin" };
    const store = createAuthStore({
      login: vi.fn().mockResolvedValue({ token: "jwt" }),
      me: vi.fn().mockResolvedValue(user),
    });

    await store.getState().login("admin", "admin123");

    expect(store.getState().token).toBe("jwt");
    expect(store.getState().user).toEqual(user);
    expect(localStorage.getItem("resume-agent-token")).toBe("jwt");
  });

  it("recovers session from localStorage token", async () => {
    localStorage.setItem("resume-agent-token", "jwt");
    const user: CurrentUser = { id: "u1", username: "hr", role: "user" };
    const store = createAuthStore({
      login: vi.fn(),
      me: vi.fn().mockResolvedValue(user),
    });

    await store.getState().recoverSession();

    expect(store.getState().token).toBe("jwt");
    expect(store.getState().user).toEqual(user);
  });

  it("clears token when session recovery fails", async () => {
    localStorage.setItem("resume-agent-token", "bad");
    const store = createAuthStore({
      login: vi.fn(),
      me: vi.fn().mockRejectedValue(new Error("unauthorized")),
    });

    await store.getState().recoverSession();

    expect(store.getState().token).toBeNull();
    expect(store.getState().user).toBeNull();
    expect(localStorage.getItem("resume-agent-token")).toBeNull();
  });

  it("logs out and clears persisted state", () => {
    localStorage.setItem("resume-agent-token", "jwt");
    const store = createAuthStore({
      login: vi.fn(),
      me: vi.fn(),
    });

    store.getState().logout();

    expect(store.getState().token).toBeNull();
    expect(store.getState().user).toBeNull();
    expect(localStorage.getItem("resume-agent-token")).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
cd repo/frontend
npm run test -- tests/auth-store.test.ts
```

Expected: FAIL because auth store files do not exist.

- [ ] **Step 3: Implement auth types and store**

```ts
// src/lib/auth-types.ts
export type UserRole = "admin" | "user";

export type CurrentUser = {
  id: string;
  username: string;
  role: UserRole;
};

export type LoginResponse = {
  token: string;
};
```

```ts
// src/lib/auth-store.ts
import { createStore } from "zustand/vanilla";
import type { CurrentUser, LoginResponse } from "@/lib/auth-types";

const TOKEN_KEY = "resume-agent-token";

type AuthApi = {
  login: (username: string, password: string) => Promise<LoginResponse>;
  me: () => Promise<CurrentUser>;
};

type AuthState = {
  token: string | null;
  user: CurrentUser | null;
  status: "idle" | "loading" | "authenticated" | "anonymous";
  error: string | null;
  login: (username: string, password: string) => Promise<void>;
  recoverSession: () => Promise<void>;
  logout: () => void;
};

export function getStoredToken() {
  if (typeof localStorage === "undefined") {
    return null;
  }
  return localStorage.getItem(TOKEN_KEY);
}

export function createAuthStore(api: AuthApi) {
  return createStore<AuthState>((set) => ({
    token: getStoredToken(),
    user: null,
    status: "idle",
    error: null,
    async login(username, password) {
      set({ status: "loading", error: null });
      const result = await api.login(username, password);
      localStorage.setItem(TOKEN_KEY, result.token);
      set({ token: result.token });
      const user = await api.me();
      set({ user, status: "authenticated", error: null });
    },
    async recoverSession() {
      const token = getStoredToken();
      if (!token) {
        set({ token: null, user: null, status: "anonymous", error: null });
        return;
      }

      set({ token, status: "loading", error: null });
      try {
        const user = await api.me();
        set({ user, status: "authenticated", error: null });
      } catch {
        localStorage.removeItem(TOKEN_KEY);
        set({ token: null, user: null, status: "anonymous", error: null });
      }
    },
    logout() {
      localStorage.removeItem(TOKEN_KEY);
      set({ token: null, user: null, status: "anonymous", error: null });
    },
  }));
}
```

- [ ] **Step 4: Run tests and checks**

Run:

```bash
cd repo/frontend
npm run test -- tests/auth-store.test.ts
npm run check
```

Expected: both commands exit with code 0.

- [ ] **Step 5: Commit**

```bash
git add repo/frontend/src/lib/auth-types.ts repo/frontend/src/lib/auth-store.ts repo/frontend/tests/auth-store.test.ts
git commit -m "feat(web): 增加登录状态管理"
```

---

## Task 4: Add Role-Aware Navigation

**Files:**

- Create: `repo/frontend/src/config/navigation.ts`
- Test: `repo/frontend/tests/navigation.test.ts`

- [ ] **Step 1: Write failing tests**

```ts
// tests/navigation.test.ts
import { describe, expect, it } from "vitest";
import { getNavigationItems } from "@/config/navigation";

describe("navigation", () => {
  it("shows HR workbench entries for user role", () => {
    const labels = getNavigationItems("user").flatMap((group) => group.items.map((item) => item.title));
    expect(labels).toContain("首页");
    expect(labels).toContain("简历分析");
    expect(labels).toContain("候选人评估");
    expect(labels).not.toContain("数据中心");
    expect(labels).not.toContain("系统设置");
  });

  it("shows admin management entries for admin role", () => {
    const labels = getNavigationItems("admin").flatMap((group) => group.items.map((item) => item.title));
    expect(labels).toContain("数据中心");
    expect(labels).toContain("系统设置");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
cd repo/frontend
npm run test -- tests/navigation.test.ts
```

Expected: FAIL because `@/config/navigation` does not exist.

- [ ] **Step 3: Implement navigation config**

```ts
// src/config/navigation.ts
import {
  BriefcaseBusiness,
  Database,
  FileText,
  History,
  Home,
  Settings,
  Upload,
  Users,
  type LucideIcon,
} from "lucide-react";
import type { UserRole } from "@/lib/auth-types";

export type NavItem = {
  title: string;
  href: string;
  icon: LucideIcon;
};

export type NavGroup = {
  label: string;
  items: NavItem[];
};

const sharedGroups: NavGroup[] = [
  {
    label: "核心工作",
    items: [
      { title: "首页", href: "/dashboard", icon: Home },
      { title: "简历分析", href: "/dashboard/analysis", icon: Upload },
      { title: "分析记录", href: "/dashboard/jobs", icon: History },
      { title: "候选人评估", href: "/dashboard/candidates", icon: Users },
    ],
  },
  {
    label: "基础数据",
    items: [
      { title: "岗位库", href: "/dashboard/jds", icon: BriefcaseBusiness },
      { title: "报告中心", href: "/dashboard/reports", icon: FileText },
    ],
  },
];

const adminGroups: NavGroup[] = [
  {
    label: "系统管理",
    items: [
      { title: "数据中心", href: "/dashboard/data", icon: Database },
      { title: "系统设置", href: "/dashboard/settings", icon: Settings },
    ],
  },
];

export function getNavigationItems(role: UserRole): NavGroup[] {
  return role === "admin" ? [...sharedGroups, ...adminGroups] : sharedGroups;
}
```

- [ ] **Step 4: Run tests and checks**

Run:

```bash
cd repo/frontend
npm run test -- tests/navigation.test.ts
npm run check
```

Expected: both commands exit with code 0.

- [ ] **Step 5: Commit**

```bash
git add repo/frontend/src/config/navigation.ts repo/frontend/tests/navigation.test.ts
git commit -m "feat(web): 增加角色导航配置"
```

---

## Task 5: Build App Shell and Protected Routes

**Files:**

- Create: `repo/frontend/src/app/globals.css`
- Create: `repo/frontend/src/app/layout.tsx`
- Create: `repo/frontend/src/app/page.tsx`
- Create: `repo/frontend/src/app/dashboard/layout.tsx`
- Create: `repo/frontend/src/app/dashboard/page.tsx`
- Create: `repo/frontend/src/app/dashboard/analysis/page.tsx`
- Create: `repo/frontend/src/app/dashboard/jobs/page.tsx`
- Create: `repo/frontend/src/app/dashboard/candidates/page.tsx`
- Create: `repo/frontend/src/app/dashboard/jds/page.tsx`
- Create: `repo/frontend/src/app/dashboard/reports/page.tsx`
- Create: `repo/frontend/src/app/dashboard/data/page.tsx`
- Create: `repo/frontend/src/app/dashboard/settings/page.tsx`
- Create: `repo/frontend/src/components/layout/app-shell.tsx`
- Create: `repo/frontend/src/components/layout/app-sidebar.tsx`
- Create: `repo/frontend/src/components/layout/top-nav.tsx`
- Create: `repo/frontend/src/components/layout/protected-route.tsx`
- Create: `repo/frontend/src/components/ui/button.tsx`
- Create: `repo/frontend/src/components/ui/badge.tsx`
- Create: `repo/frontend/src/lib/utils.ts`
- Modify: `repo/frontend/src/lib/auth-store.ts`

- [ ] **Step 1: Add utility and UI primitives**

```ts
// src/lib/utils.ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

```tsx
// src/components/ui/button.tsx
import * as React from "react";
import { cn } from "@/lib/utils";

type ButtonProps = React.ButtonHTMLAttributes<HTMLButtonElement> & {
  variant?: "primary" | "secondary" | "ghost";
};

export function Button({ className, variant = "primary", ...props }: ButtonProps) {
  return (
    <button
      className={cn(
        "inline-flex h-9 items-center justify-center rounded-md px-3 text-sm font-medium transition-colors disabled:pointer-events-none disabled:opacity-50",
        variant === "primary" && "bg-blue-600 text-white hover:bg-blue-700",
        variant === "secondary" && "border border-slate-200 bg-white text-slate-900 hover:bg-slate-50",
        variant === "ghost" && "text-slate-700 hover:bg-slate-100",
        className
      )}
      {...props}
    />
  );
}
```

```tsx
// src/components/ui/badge.tsx
import type { ReactNode } from "react";
import { cn } from "@/lib/utils";

type BadgeProps = {
  children: ReactNode;
  variant?: "success" | "warning" | "neutral";
};

export function Badge({ children, variant = "neutral" }: BadgeProps) {
  return (
    <span
      className={cn(
        "inline-flex items-center rounded px-2 py-0.5 text-xs",
        variant === "success" && "bg-green-50 text-green-700",
        variant === "warning" && "bg-orange-50 text-orange-700",
        variant === "neutral" && "bg-slate-100 text-slate-600"
      )}
    >
      {children}
    </span>
  );
}
```

- [ ] **Step 2: Add global styles and root layout**

```css
/* src/app/globals.css */
@import "tailwindcss";

:root {
  color-scheme: light;
}

body {
  margin: 0;
  background: #f6f7f9;
  color: #111827;
  font-family: Inter, "Microsoft YaHei", "PingFang SC", Arial, sans-serif;
}
```

```tsx
// src/app/layout.tsx
import type { Metadata } from "next";
import type { ReactNode } from "react";
import "@/app/globals.css";

export const metadata: Metadata = {
  title: "ResumeAgent",
  description: "ResumeAgent Web 管理面板",
};

export default function RootLayout({ children }: Readonly<{ children: ReactNode }>) {
  return (
    <html lang="zh-CN">
      <body>{children}</body>
    </html>
  );
}
```

```tsx
// src/app/page.tsx
import { redirect } from "next/navigation";

export default function HomePage() {
  redirect("/dashboard");
}
```

- [ ] **Step 3: Add placeholder pages**

Each page should use this pattern with the matching title:

```tsx
// src/app/dashboard/page.tsx
export default function DashboardPage() {
  return (
    <main className="space-y-2">
      <h1 className="text-2xl font-semibold">首页</h1>
      <p className="text-sm text-slate-500">系统总览将在 V1.1B 后逐步接入真实数据。</p>
    </main>
  );
}
```

Create these route titles:

- `/dashboard/analysis`: `简历分析`
- `/dashboard/jobs`: `分析记录`
- `/dashboard/candidates`: `候选人评估`
- `/dashboard/jds`: `岗位库`
- `/dashboard/reports`: `报告中心`
- `/dashboard/data`: `数据中心`
- `/dashboard/settings`: `系统设置`

- [ ] **Step 4: Add AppShell components**

```tsx
// src/components/layout/top-nav.tsx
"use client";

import { LogOut } from "lucide-react";
import { Button } from "@/components/ui/button";
import type { CurrentUser } from "@/lib/auth-types";

type TopNavProps = {
  user: CurrentUser;
  onLogout: () => void;
};

export function TopNav({ user, onLogout }: TopNavProps) {
  return (
    <header className="flex h-14 items-center justify-between border-b border-slate-200 bg-white px-5">
      <div className="font-semibold">ResumeAgent</div>
      <div className="flex items-center gap-3">
        <span className="text-sm text-slate-500">{user.username}</span>
        <Button type="button" variant="ghost" onClick={onLogout} aria-label="退出登录">
          <LogOut className="h-4 w-4" />
        </Button>
      </div>
    </header>
  );
}
```

```tsx
// src/components/layout/app-sidebar.tsx
"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { getNavigationItems } from "@/config/navigation";
import type { UserRole } from "@/lib/auth-types";
import { cn } from "@/lib/utils";

export function AppSidebar({ role }: { role: UserRole }) {
  const pathname = usePathname();
  const groups = getNavigationItems(role);

  return (
    <aside className="w-56 shrink-0 border-r border-slate-200 bg-slate-100 px-3 py-4">
      <nav className="space-y-5">
        {groups.map((group) => (
          <section key={group.label} className="space-y-2">
            <div className="px-2 text-xs font-medium text-slate-500">{group.label}</div>
            <div className="space-y-1">
              {group.items.map((item) => {
                const active = pathname === item.href;
                const Icon = item.icon;
                return (
                  <Link
                    key={item.href}
                    href={item.href}
                    className={cn(
                      "flex h-9 items-center gap-2 rounded-md px-2 text-sm text-slate-700",
                      active && "bg-blue-50 text-blue-700"
                    )}
                  >
                    <Icon className="h-4 w-4" />
                    {item.title}
                  </Link>
                );
              })}
            </div>
          </section>
        ))}
      </nav>
    </aside>
  );
}
```

```tsx
// src/components/layout/app-shell.tsx
"use client";

import type { ReactNode } from "react";
import { AppSidebar } from "@/components/layout/app-sidebar";
import { TopNav } from "@/components/layout/top-nav";
import type { CurrentUser } from "@/lib/auth-types";

type AppShellProps = {
  user: CurrentUser;
  onLogout: () => void;
  children: ReactNode;
};

export function AppShell({ user, onLogout, children }: AppShellProps) {
  return (
    <div className="min-h-screen bg-slate-50">
      <TopNav user={user} onLogout={onLogout} />
      <div className="flex min-h-[calc(100vh-56px)]">
        <AppSidebar role={user.role} />
        <div className="flex-1 p-6">{children}</div>
      </div>
    </div>
  );
}
```

- [ ] **Step 5: Add protected route**

Update the existing top imports in `auth-store.ts`:

```ts
import { useStore } from "zustand";
import { createStore } from "zustand/vanilla";
```

Then append the React-bound store wrapper after `createAuthStore`:

```ts
// append to src/lib/auth-store.ts

let authStore: ReturnType<typeof createAuthStore> | null = null;

export function getAuthStore(api: AuthApi) {
  authStore ??= createAuthStore(api);
  return authStore;
}

export function useAuthStore<T>(selector: (state: AuthState) => T) {
  if (!authStore) {
    throw new Error("auth store has not been initialized");
  }
  return useStore(authStore, selector);
}
```

```tsx
// src/components/layout/protected-route.tsx
"use client";

import { useRouter } from "next/navigation";
import { useEffect, type ReactNode } from "react";
import { AppShell } from "@/components/layout/app-shell";
import { createApiClient } from "@/lib/api-client";
import { getAuthStore, useAuthStore } from "@/lib/auth-store";
import type { CurrentUser, LoginResponse } from "@/lib/auth-types";

const api = createApiClient({
  baseUrl: process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8080/api/v1",
  getToken: () => getAuthStore(authApi).getState().token,
});

const authApi = {
  login: (username: string, password: string) => api.post<LoginResponse>("/auth/login", { username, password }),
  me: () => api.get<CurrentUser>("/auth/me"),
};

getAuthStore(authApi);

export function ProtectedRoute({ children }: { children: ReactNode }) {
  const router = useRouter();
  const user = useAuthStore((state) => state.user);
  const status = useAuthStore((state) => state.status);
  const recoverSession = useAuthStore((state) => state.recoverSession);
  const logout = useAuthStore((state) => state.logout);

  useEffect(() => {
    void recoverSession();
  }, [recoverSession]);

  useEffect(() => {
    if (status === "anonymous") {
      router.replace("/login");
    }
  }, [router, status]);

  if (status === "idle" || status === "loading" || !user) {
    return <div className="p-6 text-sm text-slate-500">加载中...</div>;
  }

  return (
    <AppShell
      user={user}
      onLogout={() => {
        logout();
        router.replace("/login");
      }}
    >
      {children}
    </AppShell>
  );
}
```

```tsx
// src/app/dashboard/layout.tsx
import type { ReactNode } from "react";
import { ProtectedRoute } from "@/components/layout/protected-route";

export default function DashboardLayout({ children }: { children: ReactNode }) {
  return <ProtectedRoute>{children}</ProtectedRoute>;
}
```

- [ ] **Step 6: Run build checks**

Run:

```bash
cd repo/frontend
npm run check
npm run test
npm run build
```

Expected: all commands exit with code 0.

- [ ] **Step 7: Commit**

```bash
git add repo/frontend/src/app repo/frontend/src/components repo/frontend/src/lib/utils.ts repo/frontend/src/lib/auth-store.ts
git commit -m "feat(web): 搭建后台应用壳"
```

---

## Task 6: Add Login Page

**Files:**

- Create: `repo/frontend/src/app/login/page.tsx`
- Create: `repo/frontend/src/components/auth/login-form.tsx`
- Create: `repo/frontend/src/components/ui/input.tsx`

- [ ] **Step 1: Add input primitive**

```tsx
// src/components/ui/input.tsx
import * as React from "react";
import { cn } from "@/lib/utils";

export function Input({ className, ...props }: React.InputHTMLAttributes<HTMLInputElement>) {
  return (
    <input
      className={cn(
        "h-9 w-full rounded-md border border-slate-200 bg-white px-3 text-sm outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100",
        className
      )}
      {...props}
    />
  );
}
```

- [ ] **Step 2: Add login form**

```tsx
// src/components/auth/login-form.tsx
"use client";

import { useRouter } from "next/navigation";
import { useState, type FormEvent } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { ApiError, createApiClient } from "@/lib/api-client";
import { getAuthStore, useAuthStore } from "@/lib/auth-store";
import type { CurrentUser, LoginResponse } from "@/lib/auth-types";

const api = createApiClient({
  baseUrl: process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8080/api/v1",
  getToken: () => getAuthStore(authApi).getState().token,
});

const authApi = {
  login: (username: string, password: string) => api.post<LoginResponse>("/auth/login", { username, password }),
  me: () => api.get<CurrentUser>("/auth/me"),
};

getAuthStore(authApi);

export function LoginForm() {
  const router = useRouter();
  const login = useAuthStore((state) => state.login);
  const status = useAuthStore((state) => state.status);
  const [username, setUsername] = useState("admin");
  const [password, setPassword] = useState("admin123");
  const [error, setError] = useState<string | null>(null);

  async function handleSubmit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    setError(null);
    try {
      await login(username, password);
      router.replace("/dashboard");
    } catch (err) {
      setError(err instanceof ApiError ? err.message : "登录失败");
    }
  }

  return (
    <form onSubmit={handleSubmit} className="w-full max-w-sm space-y-4 rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
      <div className="space-y-1">
        <h1 className="text-xl font-semibold">登录 ResumeAgent</h1>
        <p className="text-sm text-slate-500">使用后端账号进入管理面板。</p>
      </div>
      <label className="block space-y-1 text-sm">
        <span>用户名</span>
        <Input value={username} onChange={(event) => setUsername(event.target.value)} autoComplete="username" />
      </label>
      <label className="block space-y-1 text-sm">
        <span>密码</span>
        <Input value={password} onChange={(event) => setPassword(event.target.value)} type="password" autoComplete="current-password" />
      </label>
      {error ? <div className="rounded-md bg-red-50 px-3 py-2 text-sm text-red-700">{error}</div> : null}
      <Button type="submit" className="w-full" disabled={status === "loading"}>
        {status === "loading" ? "登录中..." : "登录"}
      </Button>
    </form>
  );
}
```

- [ ] **Step 3: Add route**

```tsx
// src/app/login/page.tsx
import { LoginForm } from "@/components/auth/login-form";

export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-50 p-6">
      <LoginForm />
    </main>
  );
}
```

- [ ] **Step 4: Run build checks**

Run:

```bash
cd repo/frontend
npm run check
npm run test
npm run build
```

Expected: all commands exit with code 0.

- [ ] **Step 5: Commit**

```bash
git add repo/frontend/src/app/login/page.tsx repo/frontend/src/components/auth/login-form.tsx repo/frontend/src/components/ui/input.tsx
git commit -m "feat(web): 增加登录页面"
```

---

## Task 7: Document Local Frontend Development

**Files:**

- Create: `repo/frontend/README.md`
- Modify: `docs/README.md`

- [ ] **Step 1: Add frontend README**

````md
# ResumeAgent Web

ResumeAgent Web 是 V1.1 管理面板前端，基于 Next.js + TypeScript + shadcn-style components。

## 本地启动

```bash
npm install
npm run dev
```

默认地址：

- Frontend: `http://localhost:3000`
- Backend API: `http://localhost:8080/api/v1`

如需修改 API 地址：

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1 npm run dev
```

## 常用命令

```bash
npm run check
npm run test
npm run build
```

## V1.1A 范围

- AppShell
- 登录鉴权
- JWT 会话恢复
- 角色导航
- API Client
````

- [ ] **Step 2: Update docs index**

Add this row to the `docs/README.md` implementation or development index:

```md
| [../repo/frontend/README.md](../repo/frontend/README.md) | 前端本地开发指南 |
```

- [ ] **Step 3: Run checks**

Run:

```bash
cd repo/frontend
npm run check
npm run test
npm run build
```

Expected: all commands exit with code 0.

- [ ] **Step 4: Commit**

```bash
git add repo/frontend/README.md docs/README.md
git commit -m "docs(web): 增加前端开发指南"
```

---

## Final Verification

Run these commands from the repository root:

```bash
cd repo/frontend
npm run check
npm run test
npm run build
```

Expected:

- `npm run check` exits with code 0.
- `npm run test` exits with code 0 and runs `api-client`, `auth-store`, and `navigation` tests.
- `npm run build` exits with code 0.

Then run:

```bash
git status --short
```

Expected: no modified V1.1A files remain unstaged or uncommitted. Existing unrelated untracked reference material may remain untracked.
